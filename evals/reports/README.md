# Evaluation pending

No pinned paired execution has run yet. This directory must not contain placeholder wins or manually entered aggregate metrics.

The final report is generated by `scripts/evaluation_report.py` from immutable raw execution rows and includes all intended, failed, and infrastructure-invalid attempts.

Reporter unit tests use in-memory data labeled `FIXTURE_ONLY — NOT A MODEL RUN`. Fixture metrics are never committed here or treated as release evidence.
